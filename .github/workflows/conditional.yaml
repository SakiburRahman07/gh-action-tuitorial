name: conditional workflow2

on:
  push:
    branches:
      - "backend/**"

jobs:
  conditional-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "conditional World"
